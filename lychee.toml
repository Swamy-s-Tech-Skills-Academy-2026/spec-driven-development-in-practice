# Lychee link checker — https://lychee.cli.rs/usage/config/
# Used by .github/workflows/ci-documentation.yml

cache = true
max_cache_age = "1d"

# Treat common success and redirect codes as valid when checking HTTP(S) links.
accept = ["200", "206", "301", "302", "308"]
